# 跨境广告监控系统 · 功能测试报告

**测试日期：** 2026-06-30
**测试环境：** Windows 11 · Python 3.13.5 · FastAPI 0.115.0

---

## 一、自动化测试（pytest）

**结果：42 / 42 通过，0 失败**

| 测试模块 | 测试数 | 结果 | 覆盖内容 |
|----------|--------|------|----------|
| `test_crud.py` | 15 | PASS | 品类 CRUD、Campaign 绑定、快照写入、告警去重、全局配置 |
| `test_routes.py` | 11 | PASS | 所有 Web 路由（设置页、品类增删改、绑定、数据页、刷新冷却） |
| `test_rules.py` | 12 | PASS | 4 条规则触发 / 不触发边界条件、去重抑制 |
| `test_wecom.py` | 4 | PASS | 4 种通知消息格式正确性 |

---

## 二、API 数据拉取模块（占位符 mock）

| 平台 | 测试项 | 结果 | 响应 |
|------|--------|------|------|
| Facebook | mock adsets/ads 数据结构 | **OK** | 3 条 AdSet，2 条 Ad |
| Google | mock adsets/ads 数据结构 | **OK** | 2 条 AdSet，1 条 Ad |
| TikTok | mock adsets/ads 数据结构 | **OK** | 2 条 AdSet，2 条 Ad |
| Facebook | 无 Token 时应抛出 RuntimeError | **OK** | `Facebook API 未配置（FACEBOOK_ACCESS_TOKEN 为空）` |
| Google | 无 Token 时应抛出 RuntimeError | **OK** | `Google Ads API 未配置（GOOGLE_ADS_DEVELOPER_TOKEN 为空）` |
| TikTok | 无 Token 时应抛出 RuntimeError | **OK** | `TikTok API 未配置（TIKTOK_ACCESS_TOKEN 为空）` |

> 三平台 API 均未接入真实账号，占位符 mock 数据正常返回，Token 缺失时已正确拦截并输出可读错误信息。

---

## 三、规则引擎

| 规则 | 触发条件 | 触发测试 | 不触发测试 | 去重测试 |
|------|----------|----------|------------|----------|
| 高消耗低 ROAS | spend≥500 且 roas≤1.2 | PASS | PASS（roas 正常 / spend 不足） | PASS（24h 内抑制） |
| 高 ROAS 可放量 | roas≥2.5 且 spend＜daily_budget | PASS | PASS（spend 已达预算上限） | PASS |
| 预算即将耗尽 | remaining≤daily_budget×10% | PASS | PASS（剩余充足） | PASS |
| 低 CTR 素材 | ctr≤0.012 且 impressions≥10000 | PASS | PASS（CTR 正常 / 展示量不足） | PASS |

---

## 四、企微通知（真实 Webhook）

**Webhook：** `https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=c4d50d0a-...`
**投手 userid：** Zhao

| 通知类型 | 结果 | 消息样例摘要 |
|----------|------|-------------|
| 高消耗低 ROAS | **[OK] 发送成功** | Facebook\|女装\|广告组-夏季连衣裙A\|消耗¥1500\|ROAS 0.90（红线1.20） |
| 高 ROAS 可放量 | **[OK] 发送成功** | Google\|3C数码\|广告组-搜索B\|ROAS 3.50（增量线2.50） |
| 预算即将耗尽 | **[OK] 发送成功** | TikTok\|家居\|广告组-信息流C\|剩余¥150（日预算¥2000） |
| 低 CTR 素材 | **[OK] 发送成功** | TikTok\|女装\|视频素材-春季上新01\|CTR 0.50%（红线1.20%） |

> 企微群已收到全部 4 条消息，@Zhao 正常触达。

---

## 五、定时调度器

| 测试项 | 结果 | 说明 |
|--------|------|------|
| `run_monitoring_job()` 空跑（无绑定） | **OK** | 无 Campaign 绑定时安全跳过，无异常 |
| `run_category_refresh(9999)` 不存在品类 | **OK** | 品类不存在时安全返回，无异常 |
| APScheduler 启动 | **OK** | 服务启动日志确认调度器已注册 `run_monitoring_job`，每小时执行 |

---

## 六、Web 界面（手动验证 + 路由测试）

| 页面 / 功能 | HTTP 状态 | 结果 |
|-------------|-----------|------|
| 品类列表页 `GET /` | 200 | **OK** |
| 系统设置页 `GET /settings` | 200 | **OK** |
| 新增品类页 `GET /categories/new` | 200 | **OK** |
| 提交新增品类 `POST /categories/new` | 303 → 200 | **OK** |
| 编辑品类页 `GET /categories/1/edit` | 200 | **OK** |
| Campaign 绑定页 `GET /categories/1/campaigns` | 200 | **OK** |
| 数据展示页 `GET /categories/1/data` | 200 | **OK** |
| 手动刷新 `POST /categories/1/refresh` | 303 → 200 | **OK** |
| 手动刷新冷却（5分钟内重复触发） | 303 带 `cooldown_` 参数 | **OK** |
| 删除品类 `POST /categories/1/delete` | 303 → 200 | **OK** |

---

## 七、数据库

| 测试项 | 结果 |
|--------|------|
| SQLite 初始化（6张表创建） | **OK** |
| 品类配置 CRUD | **OK** |
| Campaign 绑定 / 重复绑定忽略 | **OK** |
| AdSet 快照写入 + 最新快照查询 | **OK** |
| Ad 快照写入 + 最新快照查询 | **OK** |
| 告警去重（24h 内同规则不重复） | **OK** |
| 不同规则独立去重 | **OK** |
| 全局配置读写 | **OK** |

---

## 八、已知限制

| 项目 | 说明 |
|------|------|
| 三平台 API 未接入 | Facebook / Google / TikTok 均为 mock 占位符，真实数据拉取需配置 OAuth / Service Account |
| `@mention` 效果 | 企微消息已到达群组，@Zhao 是否高亮取决于企业微信后台该成员的真实 userid 是否匹配 |
| Windows 终端乱码 | 服务器日志中文乱码为 PowerShell GBK 编码问题，不影响功能 |

---

## 九、总结

**所有核心功能测试通过：**

- 42 个自动化测试全部通过（CRUD / 规则引擎 / 通知格式 / Web 路由）
- 企微真实通知推送验证：4 种告警类型全部成功到达群组
- 调度器安全空跑，异常情况正确处理
- Web 界面所有页面正常响应

**第一阶段可投入内网使用，后续只需完成三平台 API 认证接入即可开始真实数据监控。**
